/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Enable the Server Actions feature flag
    serverComponents: true, // This is an example, replace with the actual flag you want to enable
    // Other experimental flags...
  },
};

module.exports = nextConfig;
